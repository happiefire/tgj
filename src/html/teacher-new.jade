doctype html

html
  head
    title 题管家
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible",content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no, minimal-ui")
    link(rel="stylesheet", type="text/css", href="css/vendor/bootstrap.min.css")
    link(rel="stylesheet", type="text/css", href="css/main.css")


  body
    
    .app-wrap(ng-app="")
      
      .navpane-wrap
        .navpane
          nav.navpane-nav
            ul
              li
                a(style="height: 49px;") 返回
            .nav-divider(style="margin-top: 0;")
            ul
              li
                a 教师管理
              li
                a 班级与学员管理
              li
                a 统计项目
              li
                a 设置
          .brand
            hr
            .logo
              |题管家

      .main-wrap
      
        .stage-toolbar
          
        .stage
          form(role="form")
            h1 添加教师
            |填写下表以添加教师或
            button.btn.btn-default(data-toggle="modal" data-target="#myModal") 上传Excel名单
            .modal.fade#myModal(tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true")
              .modal-dialog
                .modal-content
                  .modal-header
                    button.close(type="button" data-dismiss="modal")
                      span(aria-hidden="true") &times
                      span.sr-only
                        |Close
                    h4.modal-title#myModalLabel 上传Excel名单
                  .modal-body
                    label.file-input-wrap
                      input#file-input-field(type="file" name="date")
                    p
                      |目前只接受Excel格式的名单表格(.xls)。
                      br
                      |请确保表格格式如下图所示，第一列为教师姓名，第二列为联系方式，第三列为备注。
                    img.img-thumbnail(src="img/example-excel.png" alt="示例名单" width="300px")
            table#name-list
              thead
                tr
                  th.teacher-name(scope="col") 姓名
                  th.teacher-email(scope="col") 邮箱
                  th.teacher-tel(scope="col") 手机
                  th.teacher-comment(scope="col") 备注
              tbody
                tr#name-list-row-0
                  td.teacher-name
                    input(type="text" required)
                  td.teacher-email
                    input(type="email" required)
                  td.teacher-tel
                    input(type="text")
                  td.teacher-comment
                    input(type="text")
            a.append-rows.btn 新增1行空表格
            br
            button.btn.btn-default(type="submit") 确定添加       
    
    script(type="text/javascript", src="js/vendor/jquery.min.js")
    script(type="text/javascript", src="js/vendor/bootstrap.min.js")
    script(type="text/javascript", src="js/vendor/angular.min.js")
